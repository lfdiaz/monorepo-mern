{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  },
  "scripts": {
    "start": "CI=true lerna run startDev --stream",
    "storybook": "lerna run storybook --stream"
  }
}
